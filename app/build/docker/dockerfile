# Only copying exe in, so minimal container
FROM scratch

# Default to port 80
ENV PORT 80

# Add binary
ADD bin/visitor-count /visitor-count

# Add static assets
ADD web /web

CMD ["/visitor-count"]
